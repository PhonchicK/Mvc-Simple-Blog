@model MvcSimpleBlog.Entities.Concrete.User
@{
    ViewBag.Title = "Details";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}
<!--ACTIONS-->
<section id="actions" class="py-4 mb-4 bg-light">
    <div class="container">
        <div class="row">
            <div class="col-md-3">
                <a href="@Url.Action("Delete", "AdminUser", new { Area = "admin", Model.Id })" class="btn btn-danger btn-block">
                    <i class="fas fa-check"></i> Delete
                </a>
            </div>
        </div>
    </div>
</section>
<!--END ACTIONS-->
<!-- DETAILS-->
<section id="details">
    <div class="container">
        <div class="row">
            <div class="col">
                <div class="card">
                    <div class="card-header">
                        <h4>Category</h4>
                    </div>
                    <div class="card-body">
                        @using (Html.BeginForm("Details", "AdminUser", Model.Id, FormMethod.Post))
                        {
                            @Html.AntiForgeryToken()
                            <div class="form-group">
                                <label for="Username">Username</label>
                                @Html.TextBoxFor(x => x.Username, new { @class = "form-control" })
                            </div>
                            <div class="form-group">
                                <label for="Email">Email</label>
                                @Html.TextBoxFor(x => x.Email, new { @class = "form-control" })
                            </div>
                            <div class="form-group">
                                <label for="Password">Password</label>
                                @Html.PasswordFor(x => x.Password, new { @class = "form-control" })
                            </div>
                            <div class="form-group">
                                <label for="Auth">Auth</label>
                                @Html.TextBoxFor(x => x.Auth, new { @class = "form-control" })
                            </div>
                            <input type="submit" class="btn btn-success btn-block" value="Save" />
                        }
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
<!-- END DETAILS-->